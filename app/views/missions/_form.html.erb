<% if mission.errors.any? %>
    <ul>
      <% mission.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>

<%= form_for(mission) do |b| %>

  

  <div class="field">
  <%= b.label :titls, t('title') %>
  <%= b.text_field :title %> <br />
  </div>

  <div class="field">
  <%= b.label :description, t('description') %>
  <%= b.text_field :description %> <br />
  </div>


  <%= b.submit %>
<% end %>